	
	<div @*class="modal hide <?=(isset($_GET['login'])||$this->session->flashdata('box'))?'':'fade'?>"*@ id="loginform">
	  <div class="modal-header">
	    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
	    <h3>Acesse sua conta</h3>
	    <h4 class="muted">no PreçoInfo</h4>
	  </div>
	  <form class="form-signin" @*action="<?=base_url('/login')?>"*@ method="POST" style="margin-bottom: 0;">
		<div class="modal-body">
			@*<?php /* Message Box de retorno (login por exemplo) */?>*@
			@*<?php if($this->session->flashdata('box')): ?>*@
				<div class="container-narrow">
					<div @*class="alert <?=$this->session->flashdata('box')?>"*@>
						<button type="button" class="close" data-dismiss="alert">&times;</button>
						@*<?=$this->session->flashdata('msg')?>*@
					</div>
				</div>
			@*<?php endif; ?>*@
			<div class="input-prepend">
				<span class="add-on"><i class="icon-envelope"></i></span>
	    		<input type="email" class="input-xlarge" placeholder="Email" name="username" required>
	    	</div>
	    	<div class="input-prepend">
	    		<span class="add-on"><i class="icon-key"></i></span>
	        	<input type="password" class="input-xlarge" placeholder="Senha" name="senha" required>
	        </div>
		    <a @*href="<?=base_url('esqueci-minha-senha')?>"*@>Esqueci minha senha</a>
		    <p>&nbsp;</p>
	  	</div>
	  	<div class="modal-footer">
	    	<a @*href="<?=base_url('cadastro')?>"*@ class="btn">Cadastre-se</a>
	    	<input type="hidden" name="redir" value="<?=current_url()?>">
	    	<button type="submit" class="btn btn-success"><i class="icon-signin"></i> Login</button>
		</div>
	  </form>
	  <script @*src="<?=assets_url('js')?>html5placeholder.jquery.min.js"*@ type="text/javascript"></script>
	  <script type="text/javascript" charset="utf-8">
	      jQuery(function ($) {
	          $('input[placeholder]').placeholder();
	      });
	  </script>
	</div>
	